# PRD — Lawchart(로차트): 법률 사실관계 관계도 생성기

| 항목 | 내용 |
|---|---|
| 버전 | v0.3 (제품명 Lawchart 확정·일괄 교체 — G-D 전 상표 확인에 따름) |
| 작성일 | 2026-09-05 |
| 작성/구현 | Qwen Code (사양 작성·개발·측정 보고) |
| 오너 | x1gen10 (방향 결정·Phase별 승인) |
| 상태 | **본 문서 승인이 Phase 0 착수 조건** |

## 결정 기록 (오너 확정)

| # | 결정 | 내용 |
|---|---|---|
| D1 | 제품명 | **Lawchart (로차트)** — Law + chart(관계도·차트). 법률 시장 내 동명 충돌 전무(2026-09-05 스크리닝). **[개정] 최초 확정명 Lawgram은 동일 시장 선사용(lawgram.kr "LawGram", 법률 관계도 도구) 확인으로 G-D 직전 교체 — 후보 스크리닝(Lawknot 탈락: 인도 법률 스타트업 존재) 후 확정** |
| D2 | 사용 범위 | **처음부터 공개 서비스** — 불특정 다수 대상. 따라서 원본(plot) 코드는 재사용 불가, **클린룸 재작성 전제** |
| D3 | 1차 법역 | **전 법역(민법·상법·형법·민사소송법)** — 다만 완성도 확보를 위해 Phase상으로는 민법 → 타 법역 순서로 단계 확장 |
| D4 | AI 병행 | **규칙 파서 우선, AI 후순위** — 측정으로 규칙 파서의 한계가 확인되면(Phase 4) 옵트인 AI 경로 추가 |
| D5 | KR3 재정의 (2026-09-05, G-C) | 사전 정량 목표(300+ 패턴)를 폐지하고 **커버리지 기준**으로 재정의: "등록 홀드아웃 관계 F1 ≥ 0.80 유지 + 법역별(민법·상법·형사·민소) 각 ≥ 0.80" — Phase 2 실측(전체 0.969, 법역 최저 0.944)으로 이미 충족. 사전 확장은 이후 수요 기반으로만 |

## 1. 요약

Lawchart은 법률 사실관계 지문을 붙여넣으면 당사자·목적물·권리관계를 관계도로 그려주고, 시점을 옮겨가며 그때의 권리 상태를 보여주는 공개 웹 도구다. 원형인 plot(app.yebni.cc)이 "지문 → 관계도" 수요와 사용 흐름이 실제로 통한다는 것을 확인해 주었으므로, Lawchart은 그 경험을 계승하되 (1) 전 법역 커버, (2) 측정 가능하고 공개된 정확도, (3) 비로그인·브라우저 내 처리라는 프라이버시 원칙으로 차별화한다.

## 2. 연락처 · 역할

| 역할 | 담당 | 책임 |
|---|---|---|
| 오너 | x1gen10 | 제품 방향, Phase별 승인 게이트, 우선순위·범위 변경 결정 |
| 구현 대행 | Qwen Code | PRD 유지, 클린룸 개발, 골드셋·측정 파이프라인 구축, 게이트 보고 |

## 3. 배경

### 3.1 원본 plot 분석 결과 (2026-09-05, 소스 전수 분석)

- 구조: 프레임워크 없는 단일 페이지 앱(HTML 22KB + app.js 64KB, 938줄). 관계도는 SVG, 모바일 우선(최대 폭 560px), 다크 테마, PWA.
- 무료 경로(비로그인)는 **전부 브라우저 안에서** 동작: 규칙 기반 한국어 파서(행위 동사 사전 약 62패턴, 당사자·조사·날짜·부정문 처리, 상대 날짜 환산), 원형 레이아웃 + 선 교차 최소화(2-opt), 라벨 겹침 해소, 노드 편집·핀치줌, 시점 슬라이더 타임라인(소유·점유·담보 권리 구간 계산), Tesseract.js OCR(한국어+한자), PNG 내보내기, localStorage 자동저장.
- AI 경로는 초대제: 지문을 서버로 전송해 LLM이 규칙 파서와 동일한 JSON 계약으로 결과 반환(groq → claude → workers-ai 폴백, 일일 쿼터). 사건 클라우드 저장·인증도 서버 담당.

### 3.2 원본의 한계 = Lawchart의 기회

1. **커버리지 편중** — 동사 사전이 민법 물권·채권 위준. 상법(어음·회사·보험), 형사(공소·절차), 민소법(소송 진행·집행) 이벤트는 얇다.
2. **정확도 미측정** — 어떤 지문 유형에서 파서가 실패하는지 아무 데이터가 없다. 신뢰 근거가 부족하다.
3. **서버 의존 저장** — 사건 저장·불러오기에 로그인(서버)이 필요하다. 비로그인 사용자는 브라우저를 지우면 작업이 사라진다.
4. **학습 보조 부재** — 관계도가 그려질 뿐, 사례문제 풀이에 필요한 쟁점 메모·조문 연계가 없다.

### 3.3 왜 지목받았고 왜 지금인가

법학 학습과 실무에서 사실관계 정리는 반복 노동이며, 원본이 "텍스트 한 번 붙여넣으면 관계도가 나온다"는 경험적 검증을 마쳤다. 다만 원본은 개인 프로젝트 성격(초대제 AI, 커버리지 편중)이라 공개 도구로 성장시키기 좋은 지점에 있다. 오너는 공개 서비스로 가는 것을 결정했고, 공개 목표이므로 원본 코드를 베끼지 않는 클린룸 원칙이 프로젝트 성립 조건이 된다.

## 4. 목표 · 핵심 결과 (KR)

**Objective**: 법학 학습자·실무자가 사실관계 지문을 붙여넣으면 측정 가능한 정확도의 법률관계도를 얻고, 파서가 놓쳐도 편집으로 5분 안에 완성할 수 있는 공개 웹 도구를 만든다.

| KR | 내용 | 측정 시점 |
|---|---|---|
| KR1 | 법역별 골드셋 **60건**(민법 30·상법 10·형법 10·민소 10)과 정답 라벨(당사자·관계·방향·날짜·목적물·권리변동), 자동 측정 스크립트(Node.js) 구축 | Phase 0 |
| KR2 | 민법 부분집합에서 규칙 파서 단독 **당사자 F1 ≥ 0.95, 관계 F1 ≥ 0.85** | Phase 1 종료 |
| KR3 | 전 법역 골드셋 기준 규칙 파서 **관계 F1 ≥ 0.80**, 행위 사전 **300+ 패턴**(원본 62 수준 참고), 절차 이벤트 유형 **40+** | Phase 2 종료 |
| KR4 | 파싱 누락을 수동 보완해 최종 저장까지 **지문 1건 5분 이내**(자체 사용성 점검 5케이스) | Phase 3 |
| KR5 | AI 경로(옵트인) 전 골드셋 **관계 F1 ≥ 0.92**, AI 실패 시 규칙 파서 폴백 100% | Phase 4 |

측정 지표 정의: 당사자 F1 = 검출/정답 집합의 조화평균. 관계 F1 = (당사자 쌍 + 방향 + 라벨 기본형)이 모두 일치할 때만 정답으로 세는 엄격 기준. 상세 산식은 Phase 0에서 측정 스크립트와 함께 확정한다.

## 5. 시장 세그먼트

| 세그먼트 | 일(job) | 왜 아프고(pain) | Lawchart이 주는 것(gain) |
|---|---|---|---|
| S1 로스쿨·법학과 학습자 (1차) | 사례문제·기출의 사실관계를 정리하고 관계를 파악한다 | 손그림·PPT 노동, 관계 누락, 시점별 권리 변화 추적 | 지문 붙여넣기 → 관계도·타임라인 즉시화, 쟁점 메모 |
| S2 수험생(사법고시·법무사 등) | 긴 지문을 빠르게 구조화한다 | 반복되는 같은 유형 정리 작업 | 프리셋 저장·재사용, 유형별 관계도 |
| S3 변호사·법무사 등 실무자 (2차) | 사건 사실관계를 요약해 파악·공유한다 | 사건 파일 정리 부담 | PNG·링크로 공유 가능한 요약 관계도 |
| S4 일반인 (3차) | 계약·임대차·분쟁 상황을 정리한다 | 내 상황이 법적으로 어떻게 얽혔는지 안 보임 | 전세·근저당 등 생활 법률 관계 시각화 |

제약: 모바일 웹 우선(원본과 동일하게 스마트폰에서 쓰는 경우가 많다), 기본 무료·비로그인, 첫 방문 10초 내 첫 관계도.

## 6. 가치 제안

- **핵심 문장**: "지문을 붙여넣으면 법률관계도가 된다 — 브라우저 안에서, 로그인 없이."
- 대안 대비 우위: 손그림·오피스 문서(시간), 법률 AI 챗봇(관계 구조가 아니라 문장으로만 답함), 원본 plot(법역 편중, 정확도 비공개, 저장에 로그인) 대비 — 전 법역, **공개 측정 수치**, 파일 기반 저장으로 차별화.
- 가치곡선상 밀도를 높일 요소: 파생 요소(PNG·링크·프리셋)와 신뢰 요소(정확도 게시)를 동시에 제공하고, 쟁점 메모 같은 학습 요소로 S1·S2를 붙잡는다.

## 7. 솔루션

### 7.1 UX 흐름 (와이어프레임 수준)

1. 입력: 지문 붙여넣기 / 캡처 이미지 OCR / 예시 불러오기 → "관계도 그리기"
2. 결과: 관계도(당사자 원 + 목적물 사각형 + 종류별 화살표) — 끌어 이동, 눌러 이름 수정, 확대
3. 시점: 타임라인 슬라이더 — 해당 시점에 유효한 권리만 진하게, 미래 사건은 흐리게
4. 보완: 관계 목록에서 추가·수정·삭제, 쟁점 메모
5. 저장: 브라우저 자동저장 + 파일(JSON) 내보내기/가져오기, PNG/SVG 내보내기

상세 화면 설계도는 Phase 1 산출물로 본 문서에 첨부 갱신한다(v0.2+).

### 7.2 핵심 기능 · Phase 매핑

| ID | 기능 | Phase | 비고 |
|---|---|---|---|
| F1 | 규칙 기반 파서(당사자·관계·날짜·목적물·권리효과) | 1 | 민법. 자체 사전, 원본 코드 복제 없음 |
| F2 | 관계도 편집기(드래그·이름수정·줌·자동정렬) | 1 | SVG |
| F3 | 시점 타임라인(권리 구간 계산·표시) | 1 | 소유/점유/담보 |
| F4 | 골드셋 측정 대시보드(정확도 자동 리포트) | 0→2 | 내부 도구, 결과는 공개 페이지에 게시 |
| F5 | 법역 사전 확장(상법·형사·민소 절차 이벤트) | 2 | |
| F6 | OCR 입력(Tesseract.js) | 2 | 원본 방식 계승, 재구현 |
| F7 | 저장·내보내기(localStorage + JSON 파일, PNG/SVG) | 1~3 | **서버 계정 저장은 v1 제외**(프라이버시·비용) |
| F8 | 쟁점 메모(관계·노드에 태그) | 3 | S1·S2 핵심 부가가치 |
| F9 | AI 정밀 분석(옵트인, JSON 계약 동일, 규칙 폴백) | 4 | 비용·개인정보 고지 전제 |

비목표(v1): 법률 자문·적법성 판단, 판례 검색 DB, 실시간 협업, 유료화, 네이티브 앱, 서버 계정 시스템.

### 7.3 기술 방침

- **클린룸 원칙(중요)**: `reference/`의 원본 소스(plot_source.html, plot_app.js)는 동작·사양 이해용으로만 읽는다. 코드, 정규식 표현, 상수 표를 그대로 복사하지 않는다. 파서 사전은 법률 용어 자체(매도·근저당·공소제기 등 아이디어)에서 자체 도출해 새로 작성한다. 기능은 저작권 보호 대상이 아니지만 표현은 보호 대상이므로, 구현체는 독자적으로 쓴다.
- 프레임워크 없는 바닐라 JavaScript + ES 모듈 + SVG(원본이 증명한 구조 — 빌드 없이 정적 호스팅 가능, 유지보수 단순). 다만 단일 파일이 아닌 모듈 분리(파서/사전/레이아웃/렌더/저장)로 재작성.
- 정적 호스팅(GitHub Pages 또는 Cloudflare Pages, Phase 3 결정) — 초기 운영비 0원.
- 측정 파이프라인: Node.js 스크립트(골드셋 JSON → 파서 실행 → F1 산출).
- 데이터 원칙: 기본은 전부 브라우저 처리(원본의 프라이버시 포지션 계승). 서버 전송은 AI 옵트인 시에만, 그 사실을 UI에 명시.

### 7.4 가정 (검증 필요)

| ID | 가정 | 검증 방법 | 틀렸을 때 영향 |
|---|---|---|---|
| A1 | 클린룸 작성으로 저작권 문제 회피 가능 | 필요 시 법률 검토(공개 직전) | 회피 불가 시 기획 재검토 |
| A2 | 골드셋을 공개 판례(대법원 등) 사실관계 인용으로 구성 가능 — 사례집 지문은 저작권 문제로 사용 안 함 | Phase 0에 판례 발췌 방식 확정 | 골드셋 축소·자체 지문 작성으로 전환 |
| A3 | 규칙 파서만으로 민법 관계 F1 0.85 도달 가능 | Phase 1 측정 | 미달 시 AI 도입 시점 앞당김(D4 변경) |
| A4 | 'Lawchart' 명칭의 상표·도메인 충돌 없음 | 등록 전 검색(상표권 서비스·도메인) | 명칭 변경(D1 재결정) |
| A5 | 정적 호스팅 무료 한도 내 초기 트래픽 수용 | Phase 3 배포 후 모니터링 | 호스팅 이동 또는 CDN 비용 발생 |

## 8. 출시 계획 (상대 기간, 게이트별 오너 승인)

| Phase | 범위 | 상대 기간 | 승인 게이트 |
|---|---|---|---|
| 0. 측정 기반 | 골드셋 60건 + 라벨링 기준 + 측정 스크립트 + 원본 동작 기준선(참고용) | ~2주 | **G-A**: 골드셋 구성·측정 산식·기준선 보고 승인 |
| 1. 코어 재작성 | F1~F3(민법 파서, 편집기, 타임라인) + KR2 측정 | ~3주 | **G-B**: KR2 달성 + 화면 설계도 첨부 승인 |
| 2. 전 법역 | F5·F6 + KR3 측정, 측정 결과 공개 페이지 초안 | ~4주 | **G-C**: KR3 달성 승인 |
| 3. 공개 | F7 완성, F8(쟁점 메모), 호스팅·도메인 배포, 개인정보·이용 안내 | ~2주 | **G-D**: 공개 승인(배포 전 최종 점검) |
| 4. AI 옵션 | F9 + KR5 측정, 비용 모델 | 공개 후 | **G-E**: AI 도입 승인 |

첫 공개 버전(v1) 범위 = Phase 0~3: 민법 정밀 + 타 법역 기초, 비로그인, 브라우저 처리, 파일 저장.

### 리스크 요약

1. **클린룸 이탈** — 구현 중 원본 코드를 무의식 복사할 위험. 게이트마다 참고자료 대비 이질성 점검.
2. **전 법역 사전 작업량** — D3가 범위를 넓혀 Phase 2 지연 가능. 게이트 G-C에서 법역 축소 옵션 상정.
3. **상표 충돌(A4)** — 명칭 교체 시 브랜드 재작업. Phase 3 이전 확인 필수.
4. **AI 프라이버시** — 공개 서비스에서 지문 전송은 민감. 옵트인·고지·로컬 대안 명시로 완화.
5. **골드셋 품질** — 라벨링 주관으로 측정 왜곡 가능. 라벨링 기준 문서를 Phase 0 산출로 고정.

## 부록 A. 원본 사양 인벤토리 (reference/ 기준, 사양 참고용)

- 당사자 추출: 한자(甲乙丙…)·갑을병정·단일 대문자(목적물 접두 제외)·역할어 17종·빈출 2~4글자 명사(불용어 사전 필터)
- 행위 사전: 동사 약 62패턴 [정규식, 라벨, 종류(계약·금전·담보·분쟁·신분·기타), 방향] / 통지·의사표시 이벤트 11종(타임라인 전용)
- 문장 처리: 날짜 치환 → 상대 날짜 환산(그 다음날·당일) → 절 분할(-고, -며, 한 후…) → 주어/상대/목적어 조사 매칭 → 부정문 제외 → 중복 제거
- 권리 효과: 라벨→효과(소유/매매/담보/점유) 추론, 목적물별 권리 구간(arc) 계산
- 레이아웃: 원형 배치 + 이웃 정렬 + 2-opt 교차 최소화, 목적물 내곽 배치, 라벨 겹침 해소 반복
- 부가: Tesseract.js OCR(전처리: 확대·흑백 이분화), PNG 내보내기(SVG 직렬화→canvas), localStorage 자동저장, 서버(초대제 AI·사건 저장)는 v1 제외 대상

## 부록 B. 저작권 가이드라인 (요약)

원본 plot의 코드는 @__yebni의 저작물. Lawchart은 공개 서비스이므로: (1) 코드·정규식·상수 직접 복사 금지, (2) 동작·기능·화면 구성의 아이디어는 참고 가능, (3) 파서 사전은 법률 용어로부터 자체 작성, (4) 서비스명·브랜드는 'plot'과 무관하게 유지(D1 Lawchart 확정으로 충족). `reference/` 폴더는 분석 기록 보존용이며 저장소 공개 시 제외한다.
